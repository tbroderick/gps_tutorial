To recreate the examples from the tutorial, run through the Jupyter Notebooks provided. 

If you're using pip, you can set up all the dependencies as follows:
pip install jupyterlab
pip install notebook
pip install numpy
pip install matplotlib
pip install scikit-learn